<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.talelin.latticy.mapper.PatientMapper">

    <resultMap id="BaseResultMap" type="io.github.talelin.latticy.model.PatientDO">
        <id column="id"  property="id"/>
        <result column="name"  property="name"/>
        <result column="age"  property="age"/>
        <result column="sex" property="sex"/>
        <result column="symptom" property="symptom"/>
        <result column="prescription"  property="prescription"/>
    </resultMap>

    <select id="getByName" resultMap="BaseResultMap">
        SELECT * FROM t_patient WHERE t_patient.name=#{name}
    </select>

</mapper>